import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  status: {
    danger: '#EF7D00',
  },
  palette: {
    contraste: {
      main: '#003E5C',
  },
    fond: {
      main: '#F6F8F9',
    },
}});
export default theme;